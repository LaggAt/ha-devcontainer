#!/bin/bash

# # home assistant starten nach Erstellen des Containers
# /usr/local/bin/hass --config /config --ignore-os-check --verbose

# # once done, this should work fast
# /usr/local/bin/hass --config /config --ignore-os-check --verbose --skip-pip